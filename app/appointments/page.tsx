import { ProtectedPlaceholderPage } from '@/app/components/layout';

export default function AppointmentsPage() {
  return <ProtectedPlaceholderPage title='Appointments' subtitle='View and manage appointments.' />;
}
