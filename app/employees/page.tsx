import { ProtectedPlaceholderPage } from '@/app/components/layout';

export default function EmployeesPage() {
  return <ProtectedPlaceholderPage title='Employees' subtitle='Manage staff and employees.' />;
}
